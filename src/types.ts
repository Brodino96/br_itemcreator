export type ConfigType = {
    debugMode: boolean,
    locale: string
}

export type ItemType = {
    name: string,
    label: string,
    imageUrl: string,
    description: string,
    weight: number,
    degrade: number
    decay: boolean
}